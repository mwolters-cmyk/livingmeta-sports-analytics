export const metadata = {
  title: "Feed | Living Sports Analytics",
  description:
    "Live feed of latest sports analytics research from 1,200+ monitored sources — journals, blogs, preprints, and more.",
};

export default function SourcesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
